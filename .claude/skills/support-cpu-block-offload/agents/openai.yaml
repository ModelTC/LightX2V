# English counterpart: openai_en.yaml
interface:
  display_name: "LightX2V CPU Block Offload 与权重共享"
  short_description: "接入 CPU 分块卸载与权重共享，交付默认 host 八卡、支持 NUMA 的统一入口"
  default_prompt: "使用 $support-cpu-block-offload 为 LightX2V 模型接入 CPU block offload 与共享 CPU 权重，交付可直接编辑、默认 host 八卡并通过 CLI 切换 host／NUMA 的统一启动脚本、配置、中英文指南和验证结果。"
